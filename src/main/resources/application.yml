server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: sharding
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true